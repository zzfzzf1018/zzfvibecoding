# 开发记录（AI Change Log）

> 本文件由 AI 按 `AI开发约束.md` 第 3、4 节规则维护。
> 每次改动追加一条 `CHG-YYYYMMDD-NN` 记录，**不得覆盖历史**。
> 发布级变更（版本发布）另见根目录 `CHANGELOG.md`。

---

## CHG-20260710-00
- 改动人：AI（CodeBuddy）
- 日期：2026-07-10
- 关联需求：项目初始化
- 改动目的：建立需求与设计文档体系，落实 AI 开发约束与松耦合架构基线
- 涉及文件：
  - docs/PRD.md（产品需求文档）
  - docs/SRS.md（软件需求规格说明书）
  - docs/数据源调研.md（数据源调研与选型）
  - docs/AI开发约束.md（AI 开发约束 + 留痕机制）
  - docs/架构设计文档.md（架构设计）
  - docs/软件概要设计说明书.md（概要设计）
  - docs/软件详细设计说明书.md（详细设计）
  - docs/REVIEW规范.md（评审规范）
  - docs/编码规范.md（编码规范）
  - docs/开发记录.md（本文件）
- 改动摘要（before → after）：
  - before：空仓库，无文档
  - after：完整需求/设计/约束文档体系，定义分层架构与松耦合规则
- 接口/契约影响：无（尚未编码）
- 测试情况：不适用（文档阶段）
- 风险/回滚：无代码，可整体回退删除 docs/
- 备注：确立"数据源隔离 + 依赖倒置 + 改动留痕"三大基线，供后续 AI 接手

---

## CHG-20260710-01
- 改动人：AI（CodeBuddy）
- 日期：2026-07-10
- 关联需求：架构设计文档、概要/详细设计、AI开发约束 §5（松耦合）
- 改动目的：建立可运行的代码骨架（包结构 + 空实现 + DI 装配 + 护栏测试），不写业务实现，供后续 AI 按文档逐模块填充
- 涉及文件：
  - src/main.py（FastAPI 入口 + 统一异常处理）
  - src/app/__init__.py
  - src/app/models/{schemas.py, orm.py, __init__.py}
  - src/app/core/{config.py, logging.py, errors.py, di.py, __init__.py}
  - src/app/core/clients/{http_client.py, __init__.py}
  - src/app/datasource/{interfaces.py, akshare_src.py, csindex_src.py, em_src.py, __init__.py}
  - src/app/repositories/{etf_repo.py, valuation_repo.py, constituent_repo.py, __init__.py}
  - src/app/services/{search_service.py, valuation_service.py, percentile_service.py, constituent_service.py, __init__.py}
  - src/app/api/{deps.py, router_etf.py, router_valuation.py, __init__.py}
  - src/app/scheduler/{jobs.py, __init__.py}
  - tests/{__init__.py, test_coupling.py, test_interfaces.py}
  - requirements.txt / .gitignore / README.md
- 改动摘要（before → after）：
  - before：仅有 docs/ 文档，无代码
  - after：完整分层骨架；services 仅依赖 interfaces；具体源经 di.py 注入；护栏测试就绪（业务方法均为 NotImplementedError 占位，路由未实现返回 501）
- 接口/契约影响：建立内部端口（EtfBasicSource/ValuationSource/ConstituentSource）与 HTTP 路由契约（/api/etf/...），均为新增，无破坏性变更
- 测试情况：新增 tests/test_coupling.py（静态 import 松耦合护栏）、tests/test_interfaces.py（Protocol 一致性）；需 `pip install -r requirements.txt` 后 `pytest`（环境未装依赖，未在本次执行）
- 风险/回滚：纯新增骨架，无业务数据；回滚删除 src/、tests/、requirements.txt 等即可
- 备注：遵循 AI开发约束——services 严禁 import akshare/具体源；SQL 仅 repositories；外部请求仅 datasource/core.clients。后续实现需同步补 CHG 记录并通过护栏测试

## CHG-20260710-02
- 改动人：AI（CodeBuddy）
- 日期：2026-07-10
- 关联需求：SRS FR-01~FR-06、DR-01~05、IR-02；数据源调研 §4；架构/概要/详细设计
- 改动目的：实现 1) AkShare 数据源适配器 2) repositories 真实仓储 3) services 业务逻辑（检索/估值降级/分位/成分股）4) scheduler 定时刷新；并通过 pytest 与导入冒烟测试
- 涉及文件：
  - src/app/datasource/akshare_src.py（实现 list_etfs/get_etf/get_latest/get_constituents，akshare 函数内懒加载）
  - src/app/repositories/etf_repo.py（upsert/get_basic/模糊检索/去重跟踪指数，SQLAlchemy）
  - src/app/repositories/valuation_repo.py（估值快照 upsert/get_latest/append_history/get_history）
  - src/app/repositories/constituent_repo.py（replace_constituents/get_constituents）
  - src/app/services/_etf_helper.py（resolve_etf：缺失/缺跟踪指数时经 basic_sources 补全落库）
  - src/app/services/search_service.py（编号/名称模糊检索）
  - src/app/services/valuation_service.py（估值+多源降级+不适用类 applicable=False）
  - src/app/services/percentile_service.py（分位算法，样本<250 自动降级窗口）
  - src/app/services/constituent_service.py（成分股排序/行业筛选/TopN/行业分布）
  - src/app/scheduler/jobs.py（refresh_etf_basic/valuation/constituents，单条失败不中断）
  - src/app/core/di.py（装配 services 并注入 basic_sources；启动时建表）
  - src/app/models/orm.py（补充 Optional 导入修复注解反序列化）
  - src/app/models/__init__.py（修正 re-export，移除不存在的 IndexConstituent）
  - src/app/core/__init__.py（移除 Container 导入，打破循环依赖）
  - tests/test_services.py（检索/分位/估值降级/不适用类单测）
  - pyproject.toml（pytest pythonpath=["src"]）
  - README.md（更新运行说明）
- 改动摘要（before → after）：
  - before：业务方法均为 NotImplementedError 占位，路由返回 501
  - after：各层真实实现；估值多源降级、分位计算、成分股聚合可用；DI 建表
- 接口/契约影响：services 构造函数新增 basic_sources 参数（仅影响内部装配，无对外破坏性变更）；HTTP 路由契约不变
- 测试情况：`pytest` 13 例全部通过（护栏 3 + 接口 3 + 服务 7）；应用导入冒烟测试通过（7 路由，DI 容器构建成功）。修复过程：models re-export 错误、orm 缺 Optional、core 循环依赖、单测断言笔误，均已修正
- 风险/回滚：csindex_src / em_src 仍为实现占位（抛 NotImplementedError），估值服务已做捕获降级，不阻断主流程；历史估值依赖调度自采（无稳定 AkShare 全量历史接口，见数据源调研 §4）；成分股行业(sw_l1)经 stock_individual_info_em 逐股补全（best-effort，可能受频限）
- 备注：遵循 AI开发约束——services 仅依赖 datasource.interfaces 抽象与 repositories；外部请求仅 datasource 层；新增依赖需登记。真实运行需 `pip install -r requirements.txt` 并先运行 refresh_* 或访问接口触发自采数据

<!-- 后续改动请在此处按模板继续追加 CHG-20260710-03、CHG-20260710-04 ... -->
