<Window x:Class="ValuationTools.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ValuationTools.Desktop.ViewModels"
        xmlns:views="clr-namespace:ValuationTools.Desktop.Views"
        Title="估值计算工具箱 — DCF / PEG / WACC / 相对估值"
        Height="860" Width="1440" MinHeight="640" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei UI, Segoe UI" FontSize="13"
        Background="{StaticResource PageBackgroundBrush}">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ToolViewModel}">
            <views:ToolView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧导航 -->
        <Border Background="{StaticResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="20,22,20,14">
                    <TextBlock Text="估值计算工具箱" FontSize="18" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="Valuation Toolkit" FontSize="11" Foreground="#9CA3AF" Margin="0,3,0,0" />
                </StackPanel>

                <TextBox Grid.Row="1" Margin="16,0,16,12" Padding="8,6"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="#1F2937" Foreground="White" BorderBrush="#374151"
                         CaretBrush="White"
                         ToolTip="搜索工具名称或说明" />

                <ListBox Grid.Row="2" ItemsSource="{Binding FilteredTools}"
                         SelectedItem="{Binding SelectedTool}"
                         ItemContainerStyle="{StaticResource NavItemStyle}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Foreground="#6B7280" FontSize="11"
                                               FontWeight="SemiBold" Margin="16,14,0,4" />
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                </ListBox>

                <TextBlock Grid.Row="3" Margin="16,10,16,16" TextWrapping="Wrap"
                           FontSize="11" Foreground="#6B7280"
                           Text="所有结果基于用户输入的假设计算，仅供研究参考，不构成投资建议。" />
            </Grid>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding SelectedTool}" />
    </Grid>
</Window>
