<UserControl x:Class="ComparetoolWpf.Views.WatchlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <Border DockPanel.Dock="Top" Padding="6" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Width="260" IsEditable="True" StaysOpenOnEdit="True"
                          Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                          ItemsSource="{Binding SearchResults}"
                          SelectedItem="{Binding SelectedSearch}"
                          IsTextSearchEnabled="False"/>
                <Button Content="★ 加入自选" Margin="10,0,0,0" Padding="14,4" Command="{Binding AddCommand}"/>
                <Button Content="移除选中" Margin="10,0,0,0" Padding="14,4" Command="{Binding RemoveCommand}"/>
                <TextBlock Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#666"
                           Text="自选股跨所有 Tab 共享，存于 SQLite (%LocalAppData%\ComparetoolWpf\reports.db)。"/>
            </StackPanel>
        </Border>

        <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="代码" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="市场" Binding="{Binding Market}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
