<UserControl x:Class="ComparetoolWpf.Views.MultiStockCompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ComparetoolWpf.Converters">
    <UserControl.Resources>
        <conv:NullableDoubleConverter x:Key="NumConv" />
        <conv:PercentConverter x:Key="PercentConv" />
    </UserControl.Resources>

    <DockPanel Margin="8">
        <!-- 顶部：搜索 + 选股池 + 控制 -->
        <Border DockPanel.Dock="Top" Padding="6" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ComboBox Width="260" IsEditable="True" StaysOpenOnEdit="True"
                              Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding SearchResults}"
                              SelectedItem="{Binding SearchSelectedStock}"
                              IsTextSearchEnabled="False" />
                    <Button Content="加入对比池" Margin="10,0,0,0" Padding="10,2" Command="{Binding AddStockCommand}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="对比池:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ListBox Width="500" Height="60" ItemsSource="{Binding SelectedStocks}"
                             SelectedItem="{Binding SelectedStocksItem}" />
                    <Button Content="移除" Margin="10,0,0,0" Padding="10,2" VerticalAlignment="Top"
                            Command="{Binding RemoveStockCommand}" />

                    <TextBlock Text="    报表:" VerticalAlignment="Center" Margin="20,0,4,0"/>
                    <ComboBox Width="120" ItemsSource="{Binding ReportKinds}" SelectedItem="{Binding SelectedReportKind}" />
                    <TextBlock Text="    报告期:" VerticalAlignment="Center" Margin="10,0,4,0"/>
                    <ComboBox Width="100" ItemsSource="{Binding PeriodTypes}" SelectedItem="{Binding SelectedPeriodType}" />
                    <Button Content="加载并对比" Margin="10,0,0,0" Padding="14,4" Command="{Binding LoadAndCompareCommand}" />
                    <Button Content="导出..." Margin="10,0,0,0" Padding="14,4" Command="{Binding ExportRowsCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 主体：动态列 DataGrid（在代码后置中根据 ColumnsStocks 构建） -->
        <DataGrid x:Name="ResultGrid" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  AlternationCount="2" RowBackground="White" AlternatingRowBackground="#F8F8F8" />
    </DockPanel>
</UserControl>
