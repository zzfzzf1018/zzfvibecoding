# 

**角色定义：**你是一名资深软件开发工程师，擅长金融方面的软件研发工作
**任务：**
开发一个软件，
1. 能够查找和选择大A股票，然后获取年报数据（年报，中报，季报），能够对比报表之间的差异，比如对比资产负债表、利润表、现金流良表的变化，提醒变化差异超过某个比例的指标
2. 能够选择多个股，生成百分比报表，可以对比选择的个股之间的财务报表情况
**要求：**
1. 使用C#语言，可以使用winform或者WPF作为UI层
2. 允许使用开源库
3. 要有详细的注释
4. 要有详细的文档

--

接下来的任务：
支持导出功能
已下载的报表缓存到本地，下次加载的时候如果缓存存在则不需要再次调用API。（使用SQL LITE缓存）
**指标计算**：在 `ComparisonService` 之上叠加“同比 / 环比 / 杜邦分析 / ROE 拆解”等。
**绘图**：使用 `OxyPlot` 或 `LiveCharts2` 展示趋势曲线。

----

接下来：
 **指标计算**：在 `MetricsService` 上叠加“营运资本变化、自由现金流、现金转换周期”等。2. 
搜索股票功能支持联想方便查找
思考还有什么提升易用性的功能
增加选股器TAB，可以根据基本面选股


--

接下来：
针对选股器TAB，报告期选择提供日期选择控件支持快速选择日期
针对选股器TAB，行业关键字，支持提示或者点开可以有个窗体中多选行业关键字
针对选股器TAB，解释一下最大翻页选项是干吗用的？？
针对其他tab，离慢的搜索按钮还有用吗？点击之后反而清空了？
针对单股期间对比tab，高亮阈值设置后，表格中并没有高亮显示
提供一些其他可以改进的思路
-
股票池/自选股：把"加入对比池"扩展为持久化自选股表（SQLite 已现成），跨 Tab 共享。
Excel 行高亮导出：当前 ClosedXML 仅写文本，可在导出对比表时把 IsHighlighted=true 的行同步染色，便于离线分享。
图表交互：OxyPlot 当前是静态曲线，可加 Tracker（鼠标悬浮显示数值）、对比基准归一化（首期=100）、Y 轴对数模式。
指标公式可视化：在杜邦表表头/行尾加上展开公式 popup（Tooltip 写计算口径），减少误读。
多维筛选 + 排序：选股结果支持 DataGrid 内多列排序、列筛选（已具备 Ctrl+C，再加 CanUserSortColumns=True + Filter 行）。
行业/市值聚合视图：选股结果按行业 GroupBy，显示行业 ROE/毛利率中位数，便于快速横评。
估值字段：调用 RPT_VALUEANALYSIS_DET 加入 PE/PB/PS/股息率，并支持"PE 分位"过滤——纯基本面缺一只眼。
报告期智能默认：根据当前月份自动推荐"最近一份已披露报告"（4 月→去年年报 / 9 月→今年中报…）。
缓存版本与刷新策略：缓存表加 FetchedAt 列，UI 上显示数据时间；老化策略按报告期 + 拉取时间双维度。
异常报告器：当前异常 MessageBox，建议加日志文件 (%LocalAppData%\ComparetoolWpf\logs\*.log) 方便定位接口变更。
接口降级：东方财富偶尔风控，建议加备用源（新浪、雪球）+ 重试退避。
测试：MetricsService 的同比/环比/杜邦/CCC 计算可以加单元测试项目（xUnit），确保接口字段变化时第一时间暴露。
打包：dotnet publish -c Release -r win-x64 --self-contained + PublishSingleFile=true 出一个绿色 exe，便于分发给非开发者用户
 BUG: 选股器如果选择了行业之后就查不到数据了
BUG: 选股器如果选择了报告期2026/10/7 也查不到数据了

=============

接下来:
接口降级（雪球/新浪）：要新写 2 个 Adapter 并加重试退避策略,
增加设置界面可以切换数据源

======

